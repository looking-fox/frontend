const theme = {
  primaryColor: "black",
  primaryText: "white",
  flexAllCenter: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center"
  },
  flexEvenly: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-evenly"
  }
};

export default theme;
